const {resolve} = require('path')
module.exports = {
    // output:{
    //     path:resolve(__dirname,'bundle')
    // }
    // outputDir:"pathname"
    // vue-cli 为了方便 对webpack原有的属性进行了封装，可以通过创建一个vue.config.js 文件来修改
    // 如果不能满足需求，可以通过configureWebpack的属性来编写原生的webpack配置
    // configureWebpack:{
        // 可以在这个对象中编写原生的webpack配置
        // plugins:[
            // new webpack.BannerPlugin({
                // banner:'版本注释'
            // })
        // ]
    // }
}